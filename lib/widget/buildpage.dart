import 'package:flutter/material.dart';

class BuilPage extends StatelessWidget {
  Color color;
  String urlimage;
  String title;
  String subtitle;
  BuilPage({Key? key,required this.color,required this.urlimage,required this.title,required this.subtitle }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
